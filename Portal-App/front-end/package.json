{
  "name": "my-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.0.0",
    "react-dom": "^16.0.0",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "deploy": "aws s3 sync build/ s3://tb-rosho-frontend-bucket-new-thing --acl public-read",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
  